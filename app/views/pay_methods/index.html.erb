<h1>Listing pay_methods</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @pay_methods.each do |pay_method| %>
  <tr>
    <td><%= pay_method.name %></td>
    <td><%= link_to 'Show', pay_method %></td>
    <td><%= link_to 'Edit', edit_pay_method_path(pay_method) %></td>
    <td><%= link_to 'Destroy', pay_method, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Payment method', new_pay_method_path %>
