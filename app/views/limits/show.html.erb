<h1>Limit</h1>

<p>
  <b>Team</b><br>
  <%= @limit.team_id %>
</p>

<p>
  <b>Item</b><br>
  <%= @limit.item_id %>
</p>

<p>
  <b>Amount</b><br>
  <%= @limit.amount %>
</p>

<div class="form-actions">
  <%= link_to 'Back', limits_path, :class => 'btn'  %>
  <%= link_to 'Edit', edit_limit_path(@limit), :class => 'btn' %>
  <%= link_to 'Delete', limit_path(@limit), :method => 'delete', :confirm => 'Are you sure?', :class => 'btn btn-danger' %>
</div>
